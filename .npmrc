# Copy packages from the pnpm store instead of hardlinking. Hardlinks can
# silently fail to materialize in sandboxed/containerized dev environments
# (node_modules ends up empty despite a "successful" install) — copying is
# slightly less disk-efficient but always works. Harmless on a normal machine.
package-import-method=copy
